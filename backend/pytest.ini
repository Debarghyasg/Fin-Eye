[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    -ra
    --strict-markers
    --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    asyncio: async test using pytest-asyncio
